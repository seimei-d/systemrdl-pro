#!/usr/bin/env bun
// Placeholder. Real CLI arrives in Week 4 (see docs/ROADMAP.md).
// Will spawn systemrdl-lsp as a subprocess, host @systemrdl-pro/viewer-core as a static SPA,
// and proxy `rdl/elaboratedTree` over a local websocket.
console.error('rdl-viewer: scaffold only — Week 4 deliverable.');
process.exit(2);
